# The generator options are defined once in this file.
# Each option can be overridden when using the command line tool.
# Each option option can be overridden using your own config file too.
category